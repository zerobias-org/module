openapi: 3.0.3
info:
  version: "0.0.0"
  description: "X12 EDI file receiver (DataProducer)"
  title: '@zerobias-org/module-x12-x12'
  x-impl-name: x12
  x-product-infos:
    - $ref: './node_modules/@zerobias-org/product-auditmation-generic-dataproducer/catalog.yml#/Product'
    - $ref: './node_modules/@zerobias-org/product-x12-x12/catalog.yml#/Product'
# This module implements the DataProducer interface defined in
# @zerobias-org/module-interface-dataproducer. Interface paths are referenced
# directly from the interface module; the implementation is Java (see java/).
# Adds /objects/{objectId}/download (binary) over hl7/v2: each X12 file node is
# also a binary object whose raw EDI bytes are downloadable.
# /objects/{objectId}/search (objectSearch) is deliberately NOT referenced: keyword
# or filter search across a subtree would enumerate the never-evicted /files audit
# trail in memory. Sorting, pageToken cursors and property projection are rejected
# (400) where the interface declares them; paging is by pageNumber/pageSize.
paths:
  # ---- DataProducer interface paths ----
  /objects:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects'
  /objects/{objectId}:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}'
  /objects/{objectId}/children:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1children'
  /objects/{objectId}/searchChildren:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1searchChildren'
  /objects/{objectId}/collection/elements:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1collection~1elements'
  /objects/{objectId}/collection/elements/{elementKey}:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1collection~1elements~1{elementKey}'
  /objects/{objectId}/collection/search:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1collection~1search'
  /objects/{objectId}/invoke:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1invoke'
  /objects/{objectId}/validate:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1validate'
  /objects/{objectId}/document:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1document'
  /objects/{objectId}/download:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1download'
  /objects/{objectId}/upload:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1objects~1{objectId}~1upload'
  /schemas/{schemaId}:
    $ref: './node_modules/@zerobias-org/module-interface-dataproducer/dist/module-interface-dataproducer.yml#/paths/~1schemas~1{schemaId}'
  # ---- Connection lifecycle (required for Docker-based modules) ----
  /connect:
    post:
      tags: [connection]
      summary: Establish a receiver session
      description: >-
        Binds a producer session for the given connection. The profile is
        informational and is not validated: the inbox poller and buffer are
        daemon-owned, configured by runtimeConfig, and already running; connect is
        the DataProducer handshake the pipeline uses before browse/drain.
      operationId: connect
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConnectionProfile'
      responses:
        '200':
          description: Connected
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: connected
        '400':
          description: Malformed request (no connection id, body not a JSON object)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorModel'
        '500':
          description: Unexpected failure; the cause is logged by the module, not returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorModel'
  /disconnect:
    post:
      tags: [connection]
      summary: Close the receiver session
      description: >-
        Releases the producer session. Does NOT stop the inbox poller or flush
        the buffer — those are daemon-owned and survive disconnect.
      operationId: disconnect
      responses:
        '200':
          description: Disconnected
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: disconnected
  /metadata:
    get:
      tags: [connection]
      summary: Connection metadata and health
      operationId: getConnectionMetadata
      responses:
        '200':
          description: Connection metadata
          content:
            application/json:
              schema:
                type: object
                required: [status, bufferDepth]
                properties:
                  status:
                    type: string
                    enum: [On, Error]
                    description: On while /healthz is healthy, Error while it is degraded
                  bufferDepth:
                    type: integer
                    format: int64
                    description: Un-acked transactions (new + in_flight)
  /healthz:
    get:
      tags: [health]
      summary: Daemon health probe
      description: >-
        Liveness/health for the always-on daemon. Hub Node polls this every 30s
        for daemon-mode deployments (DESIGN §9). Returns poller state per source,
        buffer depth, and database file sizes. 503 when the poller is down, under
        backpressure, or any source is unwritable, stalled, or failing every scan.
      operationId: getHealth
      responses:
        '200':
          description: Healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
        '503':
          description: Degraded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
components:
  schemas:
    ConnectionProfile:
      $ref: './connectionProfile.yml'
    ErrorModel:
      description: >-
        The platform error body (types-core errorModelBase) with the msg of
        illegalArgumentError / unexpectedError.
      type: object
      required: [key, template, timestamp, statusCode]
      properties:
        key:
          type: string
          description: Message key, e.g. err.illegal.argument, err.unexpected
        template:
          type: string
          description: Human-readable message
        timestamp:
          type: string
          format: date-time
        statusCode:
          type: integer
          format: int32
        msg:
          type: string
          description: The error message (a generic one for 500)
    HealthStatus:
      type: object
      required: [poller, db]
      properties:
        poller:
          type: object
          required: [up, bufferDepth, backpressure, sources]
          properties:
            up:
              type: boolean
              description: Every poller thread alive
            lastScan: { type: string, format: date-time }
            lastConsumed: { type: string, format: date-time }
            bufferDepth:
              type: integer
              format: int64
              description: Un-acked transactions (new + in_flight)
            oldestUnackedSec:
              type: integer
              format: int64
              description: Age of the oldest un-acked transaction; absent when there is none
            backpressure:
              type: boolean
              description: Buffer over its byte ceiling; new files are left untouched
            sources:
              type: array
              items:
                type: object
                required: [name, path, writable, pending, errored, stalled, consecutiveFailures]
                properties:
                  name: { type: string }
                  path: { type: string }
                  writable: { type: boolean }
                  pending:
                    type: integer
                    description: Candidate files not yet consumed
                  errored:
                    type: integer
                    description: The source's files rows with status error — files sent to .error and files recorded after repeated failed reads
                  stalled:
                    type: boolean
                    description: No scan completed and no file finished for 3 poll intervals + 60s
                  consecutiveFailures:
                    type: integer
                    description: Scans in a row that ended in an unexpected failure (3 = degraded)
                  lastScanStarted: { type: string, format: date-time }
                  lastScanCompleted: { type: string, format: date-time }
                  lastError:
                    type: string
                    description: The last unexpected scan failure
        db:
          type: object
          required: [walBytes, sizeBytes]
          properties:
            walBytes:
              type: integer
              format: int64
              description: Size of the WAL file
            sizeBytes:
              type: integer
              format: int64
              description: Size of the database file, free pages included
